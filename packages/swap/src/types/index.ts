export interface TokenType {
  address: string;
  symbol: string;
  decimals: number;
  name: string;
  logoURI: string;
  rank?: number;
  cgId?: string;
}

// export interface FromTokenType extends EVMTokenType{

// }
